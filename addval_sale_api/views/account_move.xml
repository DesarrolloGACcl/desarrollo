<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="account_move_sale_origin">
        <field name='name'>account.move.sale.origin</field>
        <field name='model'>account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name='type'>form</field>
        <field name='arch' type='xml'>
            <field name="partner_shipping_id" position="after">
                <field name="sale_id" string="Venta origen"/>
                <field name="pre_invoice" string="Pre-Factura"/>
            </field>
        </field>
    </record>

    <record id="action_create_payments" model="ir.actions.server">
        <field name="name">Crear pagos (emergencia)</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">code</field>
        <field name="code">
            for record in records:
                record.create_payment_in_emergency()
        </field>
    </record>
</odoo>
